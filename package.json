{
  "name": "untitled",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc && esbuild ./src/main.ts --bundle --sourcemap=inline --tsconfig=./tsconfig.json --platform=node --outfile=./dist/main.js",
    "start": "npm run build && node --enable-source-maps ./dist/main.js"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.10",
    "@types/node": "24.0.10",
    "esbuild": "0.25.5",
    "prettier": "^3.3.1",
    "typescript": "^5.2.2"
  }
}
